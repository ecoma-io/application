# testing-containers

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build testing-containers` to build the library.

## Running unit tests

Run `nx test testing-containers` to execute the unit tests via [Jest](https://jestjs.io).
