{
  "name": "nge-svg-injector",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/nge-svg-injector/src",
  "prefix": "nge",
  "projectType": "library",
  "tags": [
    "type:angular-library"
  ],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": [
        "{workspaceRoot}/dist/{projectRoot}"
      ],
      "options": {
        "project": "libs/nge-svg-injector/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/nge-svg-injector/tsconfig.lib.prod.json"
        },
        "development": {
          "tsConfig": "libs/nge-svg-injector/tsconfig.lib.json"
        }
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "libs/nge-svg-injector/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "gen-docs": {
      "executor": "@ecoma/nx-typedoc:executor"
    },
    "publish": {
      "dependsOn": ["build"],
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}/dist/{projectRoot}",
        "command": "npm publish"
      }
    }
  }
}