/**
 * Enum mức độ nghiêm trọng (severity) cho audit log.
 */
export enum Severity {
  Critical = 'Critical',
  High = 'High',
  Medium = 'Medium',
  Low = 'Low',
  Informational = 'Informational',
}
