import { IAuditLogEntryRepository } from '@ecoma/alm-domain'

/**
 * Port định nghĩa các thao tác repository cho AuditLogEntry trong lớp ứng dụng.
 * Extends port đã định nghĩa trong domain layer.
 */
export type IAuditLogEntryRepositoryPort = IAuditLogEntryRepository;
