# base-constract

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build base-constract` to build the library.

## Running unit tests

Run `nx test base-constract` to execute the unit tests via [Jest](https://jestjs.io).
